import type { Child } from "@shared/schema";
interface PortfolioChartProps {
    holdings: any[];
    child?: Child;
}
export default function PortfolioChart({ holdings, child }: PortfolioChartProps): import("react/jsx-runtime").JSX.Element;
export {};
